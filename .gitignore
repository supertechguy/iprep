__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
.env
.env.*

# Defensive: real key files should only ever live at ~/.config/iprep/,
# never inside the repo - but ignore them here too in case one lands here.
config.toml
config.local.toml
.iprep/
